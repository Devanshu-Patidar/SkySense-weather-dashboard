// Copy this file to `config.js` (do NOT commit config.js) and fill your API key.
// This project calls OpenWeather from the browser, so never use a production key here.
window.__OWM_API_KEY__ = "YOUR_OPENWEATHERMAP_API_KEY_HERE";

